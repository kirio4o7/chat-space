.side-bar
  .header
    .header-box
      %p.header-box__name
        = current_user.name
      %ul.header-box__lists
        %li.new-group
          = link_to new_group_path do
            = icon('far', 'edit', class: 'icon')
        %li.edit-user
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
      
  .groups
    - current_user.groups.each do |group|
      .group__box
        = link_to '#' do
          .group__name
            techcamp
          .group__message
            新着メッセージ
